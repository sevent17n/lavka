export const getAuthRoute = (route: string) => "/auth" + route;

export const getCategoriesRoute = (route: string) => "/categories" + route;

export const getFinancialRoute = (route: string) => "/financial" + route;

export const getOrderRoute = (route: string) => "/order" + route;

export const getProductsRoute = (route: string) => "/products" + route;

export const getUserRoute = (route: string) => "/user" + route;
